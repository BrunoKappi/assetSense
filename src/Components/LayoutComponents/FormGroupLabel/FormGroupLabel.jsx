import React from 'react';
import './FormGroupLabel.css'

const FormGroupLabel = ({ children }) => {
    return (
        <div className='FormGroupLabel'>
            {children}
        </div>
    );
};

export default FormGroupLabel;